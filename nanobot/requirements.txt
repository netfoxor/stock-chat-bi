nanobot-ai>=0.1.5.post1
pandas>=2.0
numpy>=1.24
tabulate>=0.9           # pandas.to_markdown 依赖
SQLAlchemy>=2.0
statsmodels>=0.14
# 前端：
chainlit>=1.3           # 自带 markdown / 流式 / CustomElement（承载 ECharts）
# 图表在前端由 ECharts 渲染（public/elements/EChart.jsx 从 CDN 加载 echarts），后端无需额外依赖
